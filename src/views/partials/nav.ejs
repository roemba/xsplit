<header>
	<nav class="navbar navbar-expand-lg fixed-top navbar-sticky navbar-dark bg-dark container">
		<a class="navbar-brand" href="./home">
			<img src="/assets/img/xplit-long-light.svg" width="auto" height="45px" alt="">
		</a>
		<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		
		<div class="collapse navbar-collapse">
			<ul class="navbar-nav mr-auto">
				<li class="nav-item ">
					<a class="nav-link <% if (page === 'home') { %> active <% } %>" href="/home">Home</a>
				</li>
				<li class="nav-item not-logged-in">
					<a class="nav-link <% if (page === 'login') { %> active <% } %>" href="/login">Login</a>
				</li>
				<li class="nav-item not-logged-in">
					<a class="nav-link <% if (page === 'register') { %> active <% } %>" href="/register">Register</a>
				</li>
				<li class="nav-item logged-in">
				    <a class="nav-link" href="#" id="logOutButton">Logout</a>
			</ul>

			<ul class="navbar-nav">
				<li class="nav-item logged-in">
					<a class="nav-link <% if (page === 'lists') { %> active <% } %>" href="./lists">Lists</a>
				</li>
				<li class="nav-item logged-in">
					<a class="nav-link <% if (page === 'request') { %> active <% } %>" href="/request">Request</a>
				</li>
				<li class="nav-item logged-in">
					<a class="nav-link <% if (page === 'pay') { %> active <% } %>" href="/pay">Pay</a>
				</li>
				<li class="nav-item logged-in">
					<a class="nav-link <% if (page === 'account') { %> active <% } %>" href="/account">Account</a>
				</li>
			</ul>
		</div>
	</nav>
</header>
